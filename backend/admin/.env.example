# This file is documentation only; the scripts do not load dotenv files.
# Prefer --project on every command so production mutations are explicit.
GOOGLE_CLOUD_PROJECT=YOUR_GENERATED_FIREBASE_PROJECT_ID

# Local Emulator Suite only:
FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099
FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
EMULATOR_SEED_PASSWORD=LocalDemoOnly!2026
