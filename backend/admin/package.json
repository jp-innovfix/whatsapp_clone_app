{
  "name": "innovfix-firebase-admin-tools",
  "version": "1.0.0",
  "private": true,
  "main": "lib/admin-cli.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:check": "tsc --noEmit",
    "test:unit": "node --test test/*.test.js",
    "test": "npm run test:unit && npm run build",
    "admin": "node lib/admin-cli.js",
    "seed:emulator": "node lib/seed-emulator.js"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.17.0",
    "typescript": "^5.9.2"
  }
}
