{
  "name": "innovfix-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "firebase --config ../firebase.json emulators:exec --only firestore,storage --project demo-innovfix \"npm run test:rules\"",
    "test:rules": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.0.0"
  }
}
